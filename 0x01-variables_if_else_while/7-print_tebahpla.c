#include <stdio.h>
/**
 * main - print the lowercase alphabets in reverse
 * Return: 0 (Success)
 */
int main(void)
{
char mg:

for (mg = 'z'z; mg >= 'a'; mg--)
putchar(mg);
putchar('\n');
return (0);
}
